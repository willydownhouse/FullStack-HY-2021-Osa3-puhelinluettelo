{
  "name": "puhelinluettelo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf build && cd ../FullStack-HY-2021/Osa2/puhelinluettelo/ && npm run build --prod && cp -r build ../../../FullStack-HY-2021-Osa3-puhelinluettelo",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "deploy": "git push heroku master",
    "eslint": "eslint .",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ville Alatalo",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.13.5",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@babel/core": "^7.16.0",
    "@babel/eslint-parser": "^7.15.0",
    "eslint": "^7.32.0"
  }
}
